# My Personal Portfolio

This project is a personal portfolio built with React, Tailwind CSS v4, and modern web development tools.

## 🚀 Technologies

- **Vite**: Build tool for fast development
- **React**: UI library
- **Tailwind CSS v4**: Utility-first CSS framework with native cascading
- **React Router**: For client-side routing
- **Redux Toolkit**: State management
- **React i18next**: Internationalization (Multi-language)
- **Lucide React**: Icon library
- **Axios**: HTTP client

## 📁 Project Structure

- `src/components`: Reusable UI components
- `src/pages`: Main application pages
- `src/layouts`: Page layouts
- `src/features`: Redux logic and features
- `src/store`: Global state management
- `src/locales`: Translation files
- `src/hooks`: Custom React hooks
- `src/utils`: Helper functions
- `src/services`: API services

## 🛠️ Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start development server:
   ```bash
   npm run dev
   ```
3. Build for production:
   ```bash
   npm run build
   ```

## 🧩 Path Aliases

You can use the `@` alias to import files from the `src` directory:

```javascript
import Button from "@/components/Button";
```
